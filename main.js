import add from './add';
import multiply from './multiply';

const addTotal = add(5, 8);
const multipleTotal = multiply(5, 8);

console.log(`add 5 and 8 results in ${addTotal} `);
console.log(`multiple 5 and 8 results in ${multipleTotal} `);

